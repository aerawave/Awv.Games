using Awv.Games.WoW.Tooltips.Interface;
using SixLabors.ImageSharp;
using SixLabors.ImageSharp.PixelFormats;
using System.IO;

namespace Awv.Games.WoW.Graphics
{
    public static class GraphicsExtensions
    {

        /*public static Image<Rgba32> GenerateTooltip(this ITooltip tooltip, FileStream fs, int tilesize = 16)
            => tooltip.GenerateTooltip(Image.Load(fs));

        public static Image<Rgba32> GenerateTooltip(this ITooltip tooltip, Image<Rgba32> sheet, int tilesize = 16)
        {
            var gen = new TooltipGenerator();
            gen.LoadSource(sheet, tilesize);

            return gen.Generate(tooltip);
        }*/
    }
}
